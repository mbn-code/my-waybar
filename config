{
  "layer": "top",
  "position": "top",
  "margin-top": 10,
  "margin-left": 20,
  "margin-right": 20,
  "modules-left": ["hyprland/workspaces", "hyprland/window"],
  "modules-center": ["clock"],
  "modules-right": ["mpris", "cpu", "memory", "custom/weather", "tray", "bluetooth", "pulseaudio", "network", "battery"],

  "hyprland/window": {
    "format": "{class} - {title}",
    "separate-outputs": true,
    "max-length": 40,
    "tooltip": true,
    "tooltip-format": "{class}: {title}"
  },

  "clock": {
    "format": "{:%a %d %b  %H:%M}",
    "tooltip-format": "{:%A, %B %d, %Y}"
  },

  "mpris": {
    "format": "{player_icon} {dynamic}",
    "format-paused": "{status_icon} <i>{dynamic}</i>",
    "player-icons": {
      "default": "🎵"
    },
    "status-icons": {
      "paused": "⏸",
      "playing": "▶"
    },
    "max-length": 30
  },

  "cpu": {
    "format": "󰍛 {usage}%",
    "interval": 2,
    "tooltip": false
  },

  "memory": {
    "format": "󰘚 {percentage}%",
    "tooltip-format": "Memory: {used:0.1f}G/{total:0.1f}G"
  },

  "custom/weather": {
    "format": "{}",
    "exec": "curl -s 'wttr.in/?format=1' | head -1",
    "interval": 3600,
    "tooltip": false
  },

  "tray": {
    "icon-size": 16,
    "spacing": 10
  },

  "bluetooth": {
    "format": " {status}",
    "format-connected": " {device_alias}",
    "on-click": "blueman-manager"
  },

  "battery": {
    "format": "{capacity}% {icon}",
    "format-charging": "{capacity}% ⚡"
  },

  "network": {
    "format-wifi": "📶 {signalStrength}%",
    "format-ethernet": "🖧 {ipaddr}",
    "format-disconnected": "⚠ Disconnected"
  },

  "pulseaudio": {
    "format": "🔊 {volume}%",
    "scroll-step": 5
  }
}