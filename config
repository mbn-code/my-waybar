{
  "layer": "top",
  "position": "top",
  "modules-left": ["sway/workspaces", "custom/git_branch"],
  "modules-center": ["clock"],
  "modules-right": ["cpu", "memory", "battery", "network", "pulseaudio"],

  "clock": {
    "format": "{:%a %b %d %H:%M:%S}",
    "tooltip-format": "{:%A, %B %d, %Y}"
  },

  "cpu": {
    "format": "CPU: {usage}%",
    "format-alt": "CPU: {usage}%",
    "tooltip": true,
    "max": 100
  },

  "memory": {
    "format": "RAM: {used}/{total} GB",
    "tooltip": true
  },

  "battery": {
    "format": "{capacity}% {icon}",
    "format-charging": "{capacity}% ⚡",
    "tooltip": true,
    "low-capacity": 15,
    "critical-capacity": 5
  },

  "network": {
    "format": "{ifname}: {ipaddr}",
    "tooltip": true
  },

  "pulseaudio": {
    "format": "{volume}% {muted}"
  },

  "custom/git_branch": {
    "exec": "~/.config/waybar/scripts/git_branch.sh",
    "interval": 5,
    "format": "{}",
    "return-type": "json"
  }
}
